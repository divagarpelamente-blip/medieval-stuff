{
  "Description": "\"Update App.jsx CSV import/export notifications and handleSubmit forms alerts to use localized translation text keys.\"",
  "Instruction": "\"Modify handleExportCSV, handleImportCSV, and handleSubmit alert messages to use localized dictionary values.\"",
  "ReplacementChunks": "[{\"AllowMultiple\":false,\"EndLine\":490,\"ReplacementContent\":\"    toast.success(\\n      transactions && transactions.length > 0\\n        ? t.success_export\\n        : t.success_export_empty\\n    );\",\"StartLine\":485,\"TargetContent\":\"    toast.success(\\n      transactions && transactions.length > 0\\n        ? 'Ledger exportado com sucesso!'\\n        : 'Template CSV exportado (sem dados)!'\\n    );\"},{\"AllowMultiple\":false,\"EndLine\":607,\"ReplacementContent\":\"        if (parsed.length === 0 || (parsed.length === 1 && parsed[0][0] === '')) {\\n          toast.success(t.success_import_zero);\\n          return;\\n        }\\n\\n        const headers = parsed[0].map(h => h.trim().toLowerCase());\\n        const rows = parsed.slice(1);\\n\\n        if (rows.length === 0 || (rows.length === 1 && rows[0].length === 1 && rows[0][0] === '')) {\\n          toast.success(t.success_import_zero);\\n          return;\\n        }\\n\\n        const listToInsert = [];\\n        for (let i = 0; i < rows.length; i++) {\\n          const row = rows[i];\\n          if (row.length === 1 && row[0] === '') continue; // skip empty lines\\n\\n          const tx = {};\\n          headers.forEach((header, idx) => {\\n            let val = row[idx] ? row[idx].trim() : '';\\n            // normalize header keys\\n            if (header === 'entity category' || header === 'entity_category') {\\n              tx.entityCategory = val;\\n            } else if (header === 'from (origem)' || header === 'from') {\\n              tx.from = val;\\n            } else if (header === 'tipo' || header === 'type') {\\n              tx.type = val.toLowerCase();\\n            } else if (header === 'ouro' || header === 'coins' || header === 'amount') {\\n              tx.amount = Number(val);\\n            } else {\\n              tx[header] = val;\\n            }\\n          });\\n\\n          // Validation\\n          if (!tx.type || !['income', 'expense'].includes(tx.type)) {\\n            tx.type = 'expense'; // default fallback\\n          }\\n          if (!tx.amount || isNaN(tx.amount)) {\\n     \n<truncated 6795 bytes>",
  "TargetFile": "\"c:\\\\Users\\\\silva\\\\.gemini\\\\antigravity\\\\Medieval Stuff\\\\client\\\\src\\\\App.jsx\"",
  "toolAction": "\"Localizing transaction submission and CSV alerts in App.jsx\"",
  "toolSummary": "\"Update App.jsx forms & CSV alerts\""
}